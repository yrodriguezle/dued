# Il sito vetrina È fatto per essere trovato: qui si indicizza tutto.
#
# ⚠️ Il `Disallow: /` va sull'host dell'APP di cassa, che è un'altra cosa e un'altra fase.
#    Scriverlo qui deindicizzerebbe esattamente il sito che stiamo costruendo — ed è un
#    errore che si scopre settimane dopo, guardando perché nessuno arriva.
User-agent: *
Allow: /

# ⚠️ La sitemap è generata da `src/pages/sitemap.xml.ts`, non da `@astrojs/sitemap`:
#    quell'integrazione elenca le pagine PRERENDERIZZATE, e qui non ce n'è nessuna — sono
#    tutte on-demand. Avrebbe prodotto un file valido che dichiara un sito senza pagine.
Sitemap: https://duedgusto.it/sitemap.xml

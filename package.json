{
  "name": "dued-workspace",
  "version": "1.0.154",
  "private": true,
  "type": "module",
  "description": "DuedGusto workspace - Frontend + Backend",
  "scripts": {
    "setup": "node setup-dev-environment.js",
    "//predev": "🔴 Un gancio PER SERVER e non uno solo su `dev`: `concurrently` invoca `npm run dev:*`, e npm esegue il `pre` di ciascuno — così la porta si libera anche lanciando un singolo server a mano, che è il caso in cui un orfano si incontra davvero. ⚠️ Ogni gancio nomina SOLO la propria porta: i tre partono insieme, e liberare le porte altrui sarebbe una corsa contro i fratelli appena avviati.",
    "dev": "concurrently -n backend,frontend,sito -c blue,green,yellow \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:sito\"",
    "predev:frontend": "node scripts/libera-porte.mjs 4001",
    "dev:frontend": "cd duedgusto && npm run dev",
    "predev:backend": "node scripts/libera-porte.mjs 4000",
    "dev:backend": "cd backend && dotnet run --launch-profile https",
    "predev:sito": "node scripts/libera-porte.mjs 4321",
    "dev:sito": "cd sito && npm run dev",
    "libera-porte": "node scripts/libera-porte.mjs 4000 4001 4321",
    "anteprima": "node scripts/anteprima.mjs",
    "anteprima:prove": "node scripts/anteprima.mjs --prove",
    "version:patch": "npm version patch --no-git-tag-version && cd duedgusto && npm version patch --no-git-tag-version && npm install --package-lock-only && cd .. && npm install --package-lock-only",
    "version:minor": "npm version minor --no-git-tag-version && cd duedgusto && npm version minor --no-git-tag-version && npm install --package-lock-only && cd .. && npm install --package-lock-only",
    "version:major": "npm version major --no-git-tag-version && cd duedgusto && npm version major --no-git-tag-version && npm install --package-lock-only && cd .. && npm install --package-lock-only"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
